<!-- Validation failure messages -->
<% if @ranking.errors.any? %>
  <% @ranking.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>

<div class="page-header">
  <h3>Edit Ranking #<%= @ranking.id %></h3>
</div>

<div class="row">
  <div class="col-md-12">
    <form action="/update_ranking/<%= @ranking.id %>" method="post">
      <!-- Hidden input for authenticity token to protect from forgery -->
      <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>">

      <!-- Label and input for player_id -->
      <div class="form-group">
        <label for="player_id" class="control-label">
          Player
        </label>

        <input type="text" id="player_id" name="player_id" placeholder="Player" class="form-control" value="<%= @ranking.player_id %>">
      </div>

      <!-- Label and input for scout_id -->
      <div class="form-group">
        <label for="scout_id" class="control-label">
          Scout
        </label>

        <input type="text" id="scout_id" name="scout_id" placeholder="Scout" class="form-control" value="<%= @ranking.scout_id %>">
      </div>

      <button class="btn btn-warning">
        Update Ranking
      </button>
      or
      <a href="/rankings/<%= @ranking.id %>">Cancel</a>
    </form>
  </div>
</div>
